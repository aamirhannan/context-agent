PORT=3000
MOCKS_PORT=4000
UPSTREAM_BASE_URL=http://localhost:4000

# mock (default, no key needed) | openai
LLM_PROVIDER=mock
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

LOG_LEVEL=info

# Optional. Leave unset to run the mock services from JSON fixtures.
# DATABASE_URL=postgres://mynaksh:mynaksh@localhost:5432/mynaksh
